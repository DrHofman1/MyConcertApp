buildscript {
    // Itt akkor van rá szükség, ha a google-services plugin-nel dolgozunk a Groovy DSL stílusban.
    // Kotlin DSL esetén általában a pluginManagement-be tesszük a plugin verziókat, de itt is működhet.
}

plugins {
    // Nem feltétlen kell semmi plusz, a pluginManagement megoldja a letöltést
}

